<!--<Application
    x:Class="TN_GASTRO.Mobile.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Application.Resources>
        <ResourceDictionary>
            --><!-- Merge WinUI controls once --><!--
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            --><!-- ========== Palette / Brushes dùng chung ========== --><!--
            <Color x:Key="Clr.Header">#0F2C3D</Color>
            <Color x:Key="Clr.Card">#DCE3E7</Color>
            <Color x:Key="Clr.TileBg">#FFFFFFFF</Color>
            <Color x:Key="Clr.TileText">#083A45</Color>
            <Color x:Key="Clr.Primary">#1E5270</Color>
            <Color x:Key="Clr.Danger">#CC2E24</Color>

            <SolidColorBrush x:Key="Brush.Header"  Color="{StaticResource Clr.Header}"/>
            <SolidColorBrush x:Key="Brush.Card"    Color="{StaticResource Clr.Card}"/>
            <SolidColorBrush x:Key="Brush.TileBg"  Color="{StaticResource Clr.TileBg}"/>
            <SolidColorBrush x:Key="Brush.TileText" Color="{StaticResource Clr.TileText}"/>
            <SolidColorBrush x:Key="Brush.Primary" Color="{StaticResource Clr.Primary}"/>
            <SolidColorBrush x:Key="Brush.Danger"  Color="{StaticResource Clr.Danger}"/>

            --><!-- (Tuỳ chọn) Font Oswald: nhớ có file Assets/Fonts/Oswald-Medium.ttf -->
            <!--<FontFamily x:Key="OswaldFont">ms-appx:///Assets/Fonts/Oswald-Medium.ttf#Oswald</FontFamily>-->

            <!-- ========== Style TextBox: đổi viền khi Focus ========== --><!--
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#334047"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="FontSize" Value="16"/>
                --><!--<Setter Property="FontFamily" Value="{StaticResource OswaldFont}"/>--><!--
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Grid>
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="{TemplateBinding CornerRadius}"/>
                                <ScrollViewer x:Name="ContentElement"
                                              Margin="{TemplateBinding Padding}"/>
                            </Grid>
                            <ControlTemplate.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver"/>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="Border.Opacity" Value="0.6"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Focused">
                                        <VisualState.Setters>
                                            --><!-- Khi focus: viền chuyển sang Primary --><!--
                                            <Setter Target="Border.BorderBrush" Value="{StaticResource Brush.Primary}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unfocused"/>
                                </VisualStateGroup>
                            </ControlTemplate.VisualStateGroups>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            --><!-- (Tuỳ chọn) Style Button mặc định đồng bộ font -->
            <!--<Style TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource OswaldFont}"/>
            </Style>--><!--
        </ResourceDictionary>
    </Application.Resources>
</Application>-->


<Application
    x:Class="TN_GASTRO.Mobile.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- WinUI default styles -->
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Style TextBox toàn app: đổi viền khi Focus -->
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#334047"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Grid x:Name="Root">
                                <!-- ĐÚNG: đặt VisualStateManager.VisualStateGroups trên phần tử gốc -->
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="FocusStates">
                                        <VisualState x:Name="Unfocused"/>
                                        <VisualState x:Name="Focused">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames
                                                    Storyboard.TargetName="Border"
                                                    Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame Value="#1E5270"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled"/>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="{TemplateBinding CornerRadius}"/>

                                <!-- Phần hiển thị nội dung của TextBox -->
                                <ScrollViewer x:Name="ContentElement"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden"
                                              IsTabStop="False"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>

